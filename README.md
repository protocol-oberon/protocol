# Protocol

This is a centralised repository responsible for:
- `studio-oberon` webassembly interface types `.wit` used to run installations in the browser
- `studio-oberon`, `art-*`, `press` repositories unified typst styling options
    - builds the font "Cormorant Garamond" with nix and exports it for use in academic papers.
    - re-exports "IosevkaTerm" for use in articles
